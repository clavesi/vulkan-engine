# Shaders Folder

In this folder is where you can find the shaders for the project.
The shaders are written in Slang which is then compiled into SPIR-V for use in Vulkan.
The batch and shell file can be used to manually compile a file, but CMake will automatically compile the shaders when
building the project.